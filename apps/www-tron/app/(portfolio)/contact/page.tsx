import { ContactSection } from '@/lib/components/contact-section-new'
import { contactData } from '@/lib/portfolio-data'

export default function ContactPage() {
  return <ContactSection data={contactData} />
}
