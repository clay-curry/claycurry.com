# Dovecot credentials (required)
DOVECOT_USERNAME=clay
DOVECOT_PASSWORD=changeme

# API Key for Cloudflare worker (generate a random string)
API_KEY=change-me-to-random-string

# AWS settings
AWS_REGION=us-east-1
STACK_NAME=dovecot-fargate
CPU=256
MEMORY=512
